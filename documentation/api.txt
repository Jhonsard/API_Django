1===> cest quoi une api
    + Application Programming Interface : Technologie jouant l'Interface de communiction
            entre deux app differente en code.

2===> c'est quoi une api rest
    + api REpresentationnal State Transfer : api a format facile a transferet, a stocket
        Exemple : le format json.

3===> c'est quoi un point de terminaison
    + c'est encore un url. c'est le point de terminaison.

4===> c'est quoi le format json
    + facilite le partage le stockage et la recuperation des donnees.
    d'ou le nom Javascript Object Notation.

5===> c'est quoi la validation d'une api
    + si le status code(tache) que tu attendais c'est ce que tu viens d'avoir
        alors la tu a eu ta validation

6===> c'est quoi le status code d'une api
    + de 100 a 500

7===> c'est quoi une serialization
    + Avoir des donnee au format simple comme json qui son facile a
        recuperer (Transformer les Object d'une DB en format simple (json)).

8===> c'est quoi une deserialization
    + Du format json en format initial

9===> c'est quoi une api web
    + api appelable sur internet.
    
10===> comment developper les apis